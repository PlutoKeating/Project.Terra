# AGENTS.md

给专业 Agent 的开发规范。本文件约束所有由 AI Agent 执行的项目开发、整理、修复、文档和交付工作。

---

## 1. 开工前强制阅读流程

每一个用户需求开始操刀前，Agent 必须先阅读并理解项目文档。没有完成本节阅读，不得开始修改文件。

### 1.1 必读根目录文档

每次需求开始前必须阅读：

- `README.md`
- `docs/ARCHITECTURE.md`
- `docs/API.md`

如任务明显涉及启动、环境、部署或本地运行，还必须阅读：

- `docs/QUICK_START.md`

### 1.2 必读模块文档

如果任务涉及某个二级核心模块，Agent 必须阅读该模块目录文档，以 `xxx/docs/README.md` 和 `xxx/docs/ARCHITECTURE.md` 为主：

- 前端任务：阅读 `frontend/docs/README.md`、`frontend/docs/ARCHITECTURE.md`
- Serverless API 或领域核心任务：阅读 `docs/ARCHITECTURE.md`、`docs/API.md`

如任务涉及模块启动、环境变量、脚本或部署，还必须阅读该模块的：

- `xxx/docs/QUICK_START.md`（如存在）
- `xxx/.env.example`（如存在）
- 相关脚本

如任务涉及生产部署，还必须阅读 `vercel.json`、`.env.example` 和 `supabase/schema.sql`。

### 1.3 阅读后的执行要求

Agent 必须把文档中确认的项目结构、API 约定、模块边界、环境变量和已有工作流作为实现约束。不得凭记忆、猜测或通用经验覆盖本项目文档。

如果文档缺失、过时或互相矛盾，Agent 必须先说明冲突，再基于当前代码和用户最新要求做最小必要变更。

---

## 2. 核心职责

Agent 的目标不是“尽快改完”，而是完成可追踪、可回滚、可审查并经过生产环境验证的工程变更。

必须做到：

- 先理解当前仓库结构、现有代码风格、已有文档和用户的最新要求。
- 只修改与任务直接相关的文件，不做无关重构。
- 每次改动后进行必要的静态检查、类型检查、测试或构建预检；运行时验收必须以 Vercel Production 部署和固定生产域名为准，不以本地启动结果代替生产验证。
- 在回复用户时说明做了什么、哪些检查通过、哪些检查无法执行以及原因。

---

## 3. 所有工作或修改必须通过 Git 同步到远程

### 3.1 必须提交并立即推送所有变更

对于任何本地文件的修改或增删，**必须全部进行 Git 仓库同步检查**，理解是否应当将新增文件添加到 `.gitignore`，或者添加到 commit。

```bash
git diff 
git add <changed-files>
git commit -m "<clear local commit message>"
git status --short --branch
git push origin <current-branch>
```

执行原则：

- 一个逻辑变更一个提交。
- 文档整理、结构调整、功能修改、修复问题应尽量分开提交。
- 提交信息必须说明真实意图，不允许使用 `update`、`fix`、`misc` 这类无法审查的消息。
- 提交前必须检查变更范围，避免把 `.env`、构建产物、依赖目录、缓存、本地数据库等内容纳入提交。
- 如果当前环境缺少 Git 命令，应明确告知用户，并继续保证文件变更本身可审查。
- 每个逻辑提交完成后必须立即普通推送到当前远程分支，不得把已提交但未推送的变更留到任务末尾。
- 推送前必须检查当前分支、远程地址和工作区状态；推送后必须确认本地分支与远程分支同步。
- 推送触发 Vercel 部署后，必须等待 Production 部署完成，并通过固定生产域名和 Vercel 部署/运行日志验证结果。

### 3.2 永远禁止任何 untracked 文件存在

对于任何含有文件改动的工作，必须在工作完成后**立即检查git status**
必须确保不存在“untracked files”或“uncommited且not-ignored files”

你需要思考预期之外的untracked files的来源，典型的来源是运行configure类操作后自动生成的config文件，需要你自行判断是否添加到ignore或commit

如果存在**非常确定与你先前工作完全无关的文件改动**，则需要询问用户如何后续处理

### 3.3 推送安全边界

Agent 必须主动执行普通推送。严禁执行任何会重写远程历史的强制推送，包括但不限于：

```bash
git push --force
git push --force-with-lease
git push --force origin <branch>
git push --force-with-lease origin <branch>
```

普通 push 是本项目交付和生产验证的必需步骤，不需要逐次请求 Human 确认。

---

## 4. Vercel Production 工作流

本项目使用单一 Vercel 项目托管前端和 Serverless API，`main` 是 Vercel Production 分支。Agent 在当前 `main` 分支上的标准流程为：

1. 开始修改前确认 `main` 与 `origin/main` 同步且工作区干净。
2. 按逻辑边界完成代码、数据库迁移、配置、测试和文档修改。
3. 执行静态检查、类型检查、测试和构建预检，但不启动本地前后端作为运行时验收依据。
4. 检查 diff，提交一个清晰的逻辑 commit，并立即普通推送到 `origin/main`。
5. 等待该 commit 对应的 Vercel Production 部署完成；部署失败时直接查看 Vercel 构建日志和运行日志定位问题。
6. 只通过固定生产域名 `https://terra.arr2018.dpdns.org` 验证页面、API、认证和数据流程。
7. 如发现问题，继续小步修改、提交、推送并重复生产验证，直到验收通过。

不得使用变化的 Preview URL 代替固定生产域名验收，也不得把本地开发服务器结果作为交付结论。

---

## 5. ！！！开发与生产验证规范

！！！修改前：

- 完成“开工前强制阅读流程”。
- 查看相关配置文件、入口文件、类型定义和调用链。
- 确认任务范围，避免误改其他模块。
- 检查当前工作区是否已有用户未提交改动，不得回滚不属于自己的改动。

！！！修改中：

- 保持改动小而清晰。
- 复用现有模式和依赖，不轻易引入新框架。
- 不把密钥、令牌、私有地址、个人机器路径写入仓库文档或源码。
- 不提交 `.env` 的真实内容，只维护 `.env.example` 模板。

！！！修改后：

- 执行与改动匹配的验证。
- 检查目录结构是否符合项目约定。
- **必须进行依赖列表文件与编译/部署等配置文件或脚本的更新**，严格遵循当前代码内容，不要缺失或包含旧内容
- **必须进行文档更新**，文档范围为全局文档与你修改涉及模块（前端/后端/开发者前端）的修改，严格按照你的代码修改与当前最新的代码内容更新文档，不要缺失或包含旧内容
- **必须进行 Git 仓库同步**：`git add`、`git commit` 后立即普通 `git push`，并确认工作区干净且本地分支与远程同步。
- **必须进行 Vercel Production 验证**：等待部署成功，通过固定生产域名检查真实页面和 API，并在异常时查看 Vercel 构建/运行日志。
- 回复用户时列出文件、验证结果和未完成风险。
