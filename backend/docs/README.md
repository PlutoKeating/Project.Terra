# Terra Engine — Backend

## Quick Start

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn terra_engine.main:app --reload --port 8000
```

Swagger docs: http://localhost:8000/docs
