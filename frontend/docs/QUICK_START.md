# Frontend Quick Start

## Prerequisites
- Node.js >= 20

## Setup
```bash
cd frontend
npm install
npm run dev
```

The client defaults to `http://localhost:8000/api/v1`; set `VITE_API_BASE_URL` before building when the API is hosted elsewhere.
